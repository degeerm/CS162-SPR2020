
#include <iostream>
#include <cmath>
#include <ctime>
#include <stdlib.h>
using namespace std;
	bool is_int(string);
	int get_int(string);	
	int check_input_array();
	int* create_array(int);
	void delete_array(int*);
	int* create_copy(int, const int*);
	void fill_array(int, int*);
	void print_array(int, int*);
	void print_prime(int, int*);
	int* sort_array(int, const int*);
